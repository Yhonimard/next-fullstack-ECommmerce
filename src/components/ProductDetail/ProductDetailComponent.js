import {
  BottomBox,
  Box,
  Container,
  ContentBox,
  DescText,
  Img,
  Price,
  Title,
} from "./styled";

const ProductDetailComponent = () => {
  const img = "https://source.unsplash.com/500x500";

  return (
    <div className="container">
      <div className="">ProductDetailComponent</div>
    </div>
  );
};
export default ProductDetailComponent;

export const getStaticPath = (ctx) => {};

export const getStaticProps = (ctc) => {};
